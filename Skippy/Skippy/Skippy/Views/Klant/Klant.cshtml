@model KlantViewModel

@{
    ViewData["Title"] = Model.naam;
}

@addTagHelper *, Skippy
<div class="row">
    <div class="col-8">
        <h1>@Model.naam</h1>
        <p>Orders van @Model.naam</p>

        <div class="rounded bg-white shadow-sm">
            @Html.Partial("_OrderLijst", Model.orders)
        </div>
        <form asp-controller="Order" asp-action="AddKlant" asp-route-klantId=@Model.id>

            <div class="form-group">
                <input type="submit" value="Toevoegen aan huidige Order" class="btn btn-primary" />
            </div>
        </form>
        <div>
            <a asp-action="Edit" asp-route-id=@Model.id>Klant wijzigen</a><br />
        </div>
    </div>

    <div class="col-4">
        <vc:order />
    </div>
</div>



